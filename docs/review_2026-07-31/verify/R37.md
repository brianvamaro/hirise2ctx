# Verification: R37 — README / SHERLOCK_RUN still instruct the next session to run the aborted F build

- **Verdict:** CONFIRMED (all three sub-claims survive; the register **understates** the citation
  count and **overstates** the blast radius)
- **Corrected severity:** medium *(register said `high (operational)`)*
- **Corrected liveness:** live-shipped *(sub-claim (ii) is live-active-plan)*
- **Verified at commit:** `da884c7`
- **Date:** 2026-08-04

## What I checked

Docs read in full at HEAD: `README.md` (all 569 lines), `SHERLOCK_RUN.md:1-60` + `:570-700`
(Parts I/J + surroundings), `ROADMAP.md` (all), `PLAN_RegionalMap.md:300-346` (§10 tail),
`DECISIONS.md:5000-5030` (gate-1 re-scope entry) and `:5490-5570` (the 2026-07-30b abort entry) +
its tail, `PLAN_FBuild.md:325-350`, `reports/map_fbuild/README.md`, `docs/review_2026-07-31/docs-consistency.md:1-165`.

Git (read-only): `git log --oneline -5 --` on each of the four docs; `git show 41a6f26 -- ROADMAP.md`;
`git show da884c7 -- ROADMAP.md` (full diff + commit message); `git show --stat 41a6f26`;
`git show --stat da884c7`; `git log --follow -- reports/figures/fbuild_gate1_summary.csv`;
`git show afe6fce:reports/figures/fbuild_gate1_{summary,windows,tiles}.csv`;
`git show 41a6f26 -- scripts/f_region_gates.py`; `git status --porcelain` on all six docs (all clean).

Greps: `0.1222`, `0.0676`, `1.65`, `1.528|0.1205` across `*.md`; `abort|2026-07-30|CLOSED` in
`README.md`, `SHERLOCK_RUN.md`, `PLAN_RegionalMap.md` (**zero hits for "abort" in all three**).

Numeric re-derivation: a pandas snippet
(`scratchpad/r37_check.py`, run via `conda run -n geospatial`) recomputing the gate-1 mosaic medians
directly from `fbuild_gate1_windows.csv` / `fbuild_gate1_tiles.csv` at both `afe6fce` and HEAD,
independently of the summary CSV.

## Numbers re-derived

Gate-1 **mosaic** row — cited in docs vs. re-derived from the per-window artifact:

| quantity | docs cite (README:382-383, SHERLOCK:639-640, DECISIONS:5010/5022, PLAN_FBuild:337) | `afe6fce` artifact (re-derived) | **HEAD `da884c7` artifact (re-derived)** | agrees with the file it names? |
|---|---|---|---|---|
| median-window η² | **0.1222** | 0.122160 | **0.120535** | ✗ |
| null p95 (median) | **0.0676** | 0.067640 | **0.070010** | ✗ |
| excess (median) | +0.0719 | 0.071880 | **0.070800** | ✗ |
| ratio (median) | **1.65** | 1.649700 | **1.527600** | ✗ (−7.4 %) |
| frac windows < 0.05 | 21.4 % | 0.213675 | **0.217949** | ✗ |
| tile-scale η² (median) | 0.18499 (DECISIONS:5029) | 0.184986 | **0.180379** | ✗ |
| tile ratio (median) | 1.5081 (implicit) | 1.508133 | **1.536774** | ✗ |
| `passes_bar` | False | False | False | ✓ (verdict unchanged) |
| mosaic cells scored | — | 51,463,928 | **49,959,090** (−2.9 %) | — |
| rows in `fbuild_gate1_summary.csv` | "banked" baseline | **`mosaic` only** | `F_full`,`F_h1only`,`F_pfree`,`F_resid`,`mosaic` | — |

Every summary-CSV field reproduces exactly from the windows CSV at both revisions, so the file was
genuinely rewritten, not mis-rounded. Mechanism confirmed at `scripts/f_region_gates.py:78-84`
(`# ONE common footprint across rows (review 2026-07-29)` → `mask = fg.common_finite(*rows.values())`),
added by `41a6f26`; per-tile `common_mask_frac` at HEAD ranges 0.916–0.999 against `raw_coverage` ≈ 1.0
for the mosaic, which is exactly the −2.9 % cell loss.

## Verdict reasoning

This is category **(f) — real and correctly stated**, for all three sub-claims. Adjudicated separately:

**(i) README + SHERLOCK_RUN Part J are pre-abort — CONFIRMED, verbatim.**
`git log` confirms both files were last touched at `458168f` (2026-07-29); the abort landed at
`41a6f26` (2026-07-30), whose `--stat` touches `DECISIONS.md`, `PLAN_FBuild.md`, `ROADMAP.md`,
`.gitignore` and `reports/` — **not** README or SHERLOCK_RUN. `da884c7` touches `ROADMAP.md` only
(4 lines). Both files are clean in the working tree, so HEAD is what a reader sees. `grep -i abort`
returns **zero** hits in either file, so the category-(b) refutation ("a prominent banner warns the
reader") is dead. Offending passages, verbatim:

- `README.md:14` — `**Current phase (2026-07): regional deployment + striping-artifact Phase 2 (invariance & leveling).**`
  (PLAN_StripingArtifact is CLOSED at `ROADMAP.md:19`.)
- `README.md:34-36` — `**H1+H4 is the first stack to reach the reopening bar** (eta² ≲ 0.05 at skill ≥ −0.02); the 907-frame build is planned in [PLAN_FBuild.md](PLAN_FBuild.md), gated on its §0 checklist.`
- `README.md:94-98` — `**Next priorities:** **PLAN_FBuild** is fully built and now needs *running*. … The only thing between here and the F-build verdict is the ~2 GB npz transfer plus ~30 min of laptop compute — see **SHERLOCK_RUN Part J** for the exact order.`
- `README.md:100-102` — `If the gates fail → fall back to shipping the A1 map + caveat + H6 provenance. Then the parked validation legs resume on the final map`
- `README.md:370-371` — `Stage D writes into **reports/map_fbuild/** — never reports/map_region/, which stays on disk as the comparison object.` (polarity inverted post-abort; the only in-repo correction is `reports/map_fbuild/README.md`, which does carry a correct `**NOT THE DELIVERABLE**` banner — but it is a directory README, not the entry point.)
- `SHERLOCK_RUN.md:599-601` — `**Stage B is COMPLETE: 906/907 npzs** … the whole remaining build is a transfer plus ~30 minutes of local compute.`
- `SHERLOCK_RUN.md:603-655` — Part J steps 1–5, numbered imperative, including `**5. §5.1 comparison.**` → `~5-7 GPU-h on the 9 CTX-equipped tiles` (`striping_a1_map.py`) + `~1 h GPU` (`striping_a1_loio.py`).

**(ii) PLAN_RegionalMap still documented as blocked on the F map — CONFIRMED, verbatim.**
`ROADMAP.md:12` is byte-identical across `41a6f26` and `da884c7` (I diffed both): it still ends
`remaining thermal legs wait for the **final (post-mitigation) map**`. `PLAN_RegionalMap.md` was last
touched at `efe545e` (2026-07-13) and carries no abort marker; `:324-325` says *"The remaining
quantitative thermal legs resume on whichever final map the mitigation decision produces"* and
`:327-331` pins that map to PLAN_FBuild: *"the 907-frame per-frame build is planned in
[PLAN_FBuild.md](PLAN_FBuild.md) … When that map ships, the parked legs resume **on it**"*. So the
charitable reading ("final map" = the mosaic) is closed off by the plan the ACTIVE row links to.
`da884c7`'s own commit message asserts the opposite ("its thermal / Rodriguez-2016 legs are
**unblocked**") — the intent never landed in either file.

**(iii) Gate-1 baseline silently overwritten — CONFIRMED, numerically exact, and the register
UNDERSTATES it.** See the table. The finding says "three docs"; there are **four documents / five
citation sites**: `README.md:382-383`, `SHERLOCK_RUN.md:639-640`, `DECISIONS.md:5010` (a table row)
**and** `:5022`, plus **`PLAN_FBuild.md:337`** (`median-window η² **0.1222**, null p95 **0.0676**,
ratio **1.65**, passes_bar False`) — which the finding missed, and which matters more than the
others because PLAN_FBuild is the *closed* plan the abort record points at. `DECISIONS.md:5022`
names `fbuild_gate1_summary.csv` as the evidence; that file at HEAD holds 0.120535/0.07001/1.5276.
The abort entry (`DECISIONS.md:5495-5560`) lists 4 explicit "Corrections to earlier readings"; the
re-scored mosaic baseline is not among them, and its gate table at `:5505-5506` quietly prints the
*new* values (η² 0.121, ratio 1.528) two pages below the old ones — so DECISIONS carries both without
reconciling them.

Also verified: the operator-facing claim *"Gate 1's mosaic baseline is already banked … The F rows
are what this run adds"* (`SHERLOCK_RUN.md:639-640`, echoed at `README.md:99`) is **false at the code
level**, not merely stale. `scripts/f_region_gates.py:73` re-reads and re-scores the mosaic
unconditionally on every run (`rows = {"mosaic": fg.read_layer(ref)}`) and writes it back into the
same summary CSV — there is no frozen pre-registered baseline. Confirmed empirically: the summary CSV
at `afe6fce` contained **only** the `mosaic` row; at HEAD it contains five, with the mosaic row moved.

**Refutations I tried and rejected:**
- *(b) a banner warns the reader* — no. Zero `abort` hits in README, SHERLOCK_RUN, PLAN_RegionalMap.
- *(c) deliberate/documented* — no. `ROADMAP.md:50-51` and CLAUDE.md's closing line both require
  updating a diverged doc in the same change; `ROADMAP.md`/`PLAN_FBuild.md` **were** updated at
  `41a6f26`, so the omission is an oversight, not a policy.
- *(a) factually wrong about the numbers* — no; every figure reproduces to 6 dp from the raw windows CSV.
- *rounding explains sub-claim (iii)* — no; 1.6497 → 1.5276 is 7.4 %, and `frac_windows_below_bar`
  moved 0.2137 → 0.2179 in the opposite direction to η², which rounding cannot do.

## Corrected claim

The register's text is accurate as far as it goes. Two corrections:

1. **"three docs cite numbers the file they name no longer contains"** → **four documents, five
   sites**: `README.md:382-383`, `SHERLOCK_RUN.md:639-640`, `DECISIONS.md:5010` **and** `:5022`, and
   **`PLAN_FBuild.md:337`**.
2. **Severity `high (operational)` → `medium`, and the stated failure scenario is over-stated.**
   The mechanism is real; the "a fresh session burns 6–8 GPU-h re-deriving an aborted verdict" path
   requires bypassing *two* authorities that a session reads **before** README:
   - `CLAUDE.md` explicitly routes current phase to `ROADMAP.md`, whose CLOSED table's **first row**
     (`:18`) reads `**CLOSED — HARD ABORT 2026-07-30 (Brian)**` in bold, and whose ACTIVE table now
     has exactly one row;
   - the always-loaded memory index names `project_state_2026-07-30` as **CURRENT** with the words
     "F BUILD HARD-ABORTED" in its own title.
   README:15-16 even self-defers to those two (`See ROADMAP.md … and the live project_state_* memory
   notes for session state`). So no shipped number, scientific verdict or live-path behaviour is
   affected, and the realistic cost is confusion/re-orientation time, not compute. That is a "real
   protocol defect with bounded impact" = **medium**, not "wrong results in a plausible scenario".

   It is *not* `low`, for two reasons that keep teeth in it: (a) README/SHERLOCK are the only
   entry-point docs an **external** reader or a fresh clone sees — the memory notes are user-local and
   travel with neither; and (b) sub-claim (ii) means the one ACTIVE plan reads as having no runnable
   work, which is a live blocker on the only open workstream, and `ROADMAP.md` is precisely the doc
   the memory-note defence relies on.

## Fix assessment

The register's proposed fix is correct but incomplete. Concrete edit list so a future session need
not re-derive anything:

1. `README.md:12-36` — replace the Status block: current phase = **"F build HARD-ABORTED 2026-07-30
   (`41a6f26`); the mosaic map in `reports/map_region/` is the deliverable; A1 stands as the
   documented partial mitigation; PLAN_StripingArtifact + PLAN_H4_Leveling CLOSED (`da884c7`)"**.
2. `README.md:94-104` — replace "Next priorities" wholesale: the only open workstream is
   PLAN_RegionalMap's thermal legs, on the **mosaic** map. Delete "PLAN_FBuild is fully built and now
   needs *running*", the "~2 GB transfer + ~30 min" estimate, and the "see SHERLOCK_RUN Part J" pointer.
3. `README.md:336-387` — keep the F runbook only if re-headed
   `**HISTORICAL — PLAN_FBuild CLOSED (HARD ABORT 2026-07-30); retained as the record of how it was
   run**`; fix `:370-371`'s polarity (`map_region` is the product, `map_fbuild` the abandoned arm —
   the wording already exists in `reports/map_fbuild/README.md` and can be reused); and strike the
   `:383-387` instruction to generate the §5.1 A1 row (`striping_a1_map.py`, ~5–7 GPU-h) — the
   comparison it feeds is moot, and per **R07** that path is train/deploy-inconsistent anyway.
4. `SHERLOCK_RUN.md` — dated `**CLOSED — F build aborted 2026-07-30**` banner at the head of
   Parts E–J (Part J is `:597`); it does *not* need deleting, it is a good record of how the build ran.
5. `ROADMAP.md:12` — amend the ACTIVE row to "remaining thermal legs **unblocked 2026-07-30** — they
   run on the **mosaic** map (`reports/map_region/`), the settled deliverable".
6. `PLAN_RegionalMap.md` §10 — add a dated `**UPDATE 2026-07-30**` note superseding the 2026-07-13
   relaunch note's premise (`:327-331`), and re-point leg 1's pre-declared "not degraded" gate, which
   was written against a leveled map that will never exist.
7. Gate-1 baseline, **four** sites not three: add one line to `DECISIONS.md`'s 2026-07-30b
   "Corrections" list — *"the 0.1222 / 0.0676 / 1.65 baseline banked at `afe6fce` was measured on the
   mosaic's own footprint; the common-footprint fix (`41a6f26`, `f_region_gates.py:78-84`) re-scores
   it to 0.120535 / 0.07001 / 1.5276 (tile 0.1804 / 1.5368), which is what `fbuild_gate1_summary.csv`
   now holds; the verdict (`passes_bar False`) is unchanged"* — then update `README.md:382-383`,
   `SHERLOCK_RUN.md:639-640` and **`PLAN_FBuild.md:337`** to the on-disk pair. Drop "the mosaic
   baseline is already banked / the F rows are what this run adds": `f_region_gates.py:73` re-scores
   the mosaic every run into the same file.
