# ROADMAP — plan index for hirise2ctx

Single map of every `PLAN_*.md` and meta-doc, with current status and the supersession chain.
`CLAUDE.md` is the project spec; `DECISIONS.md` is the authoritative running log (exact dates,
commits, numbers). The `PLAN_*.md` files sit between them — architecture-level plans for each phase.
*Last reorganized 2026-06-19; statuses refreshed 2026-08-28 (map extension opened; PLAN_MapValidation opened).*

## 🟢 ACTIVE plans (current work)

| Plan | What it is | Status |
|---|---|---|
| [PLAN_Rebuild.md](PLAN_Rebuild.md) | **The batched v2 rebuild** — executes the 12-step DAG in [docs/CODE_REVIEW_AUDIT_2026-08-06.md](docs/CODE_REVIEW_AUDIT_2026-08-06.md): commands, roots, verification gates, abort conditions, and the local/Sherlock split | ✅ **COMPLETE — all 12 steps EXECUTED and verified (2026-08-20 → 25).** Rich prevalence **0.373272** as predicted; the frozen recipe **transfers unchanged** (median per-image AUC −0.0087, inside one SE; pooled PR-AUC 0.7826, prec@5% 0.9638). Step 11 shipped **both arms at 26/26**, 156/156 rasters sha256-verified, 26/26 cell-for-cell co-registered. **Step 12 promoted them to `reports/map_region` / `reports/map_a1`** (pre-R01 product archived as `map_region_g1`), built the six regional mosaics on a footprint that **closes exactly** (56,865,526 = 26×1479² − 7,940 intra-tile nodata), passed **12/12 sidecar gates**, re-fetched THEMIS onto the corrected lattice (`assert_coregistered` dx=dy=0), and **re-derived every §6 number** — including the η² that had never been re-derived: raw like-for-like **0.2327 → 0.1298 (−44%)** at a **−0.0024** skill cost (banked: −28% at −0.024), no THEMIS-ρ cost — ⚠ **but η² relative to its own rotation null did NOT improve** (median ratio 1.599 → 1.639, better on 106/234 windows): A1 narrows the BULK of the field (`prob_raw` IQR ratio 0.85, sd ratio 1.03 -- not a uniform compression), lowering the geological floor along with the artifact, so it stays a *partial* mitigation. Notebook 29 compares all three shipped maps. DECISIONS 2026-08-24d → 2026-08-25h. Four design decisions ruled in DECISIONS 2026-08-18b, three more 2026-08-19: build **in place** gated on the SHA-256 backup pass, land the `rasterio.open` hoist before step 11, **FM path only** (no GBM sweep / W1 atlas — PENDING_REBUILD rows 2–3 stay open by that ruling). A **WAYPOINT** — v3 (BoulderNet retrain + more diverse locations) forces another rerun regardless |
| [PLAN_RegionalMap.md](PLAN_RegionalMap.md) | Regional circum-Chryse abundance map + thermal/Rodriguez-2016 validation legs (first real off-HiRISE deployment of the frozen head + CalibrationLayer) | **ACTIVE — the blocker is cleared.** Map shipped (26 tiles, Sherlock) and, since the 2026-08-25 rebuild, **promoted on the corrected global lattice with an A1 arm beside it** (`reports/map_region` / `reports/map_a1`, six regional mosaics, footprint closes exactly). MOLA leg done. **THEMIS night-IR has been re-fetched onto the new lattice** (`assert_coregistered` dx=dy=0; the pre-R01 raster is archived as `themis_night_ir_region_g1.tif` and is confirmed −100/+80 m out, so leg-1 was previously index-comparing displaced cells). Leg-1 remains weak: per-tile median ρ **0.0653 baseline / 0.0654 A1** on `prob_raw` — A1 costs nothing thermally. ⚠ That per-tile median is **not** the same statistic as leg-1s pooled mosaic ρ. **The pooled leg HAS now been re-run, co-registered, 2026-08-28** (notebook 24 re-executed on the promoted map): ρ = **+0.052** at the pixel, **+0.066** at ~1.3 km, **+0.063** at ~10 km — *marginally lower* than the 2026-06-19 displaced run (~+0.06 / ~+0.07), so misregistration was **not** what was holding leg 1 down and R01's per-tile +0.0741→+0.0821 lift does not carry to the pooled mosaic (DECISIONS 2026-08-28a). **Remaining thermal legs are now unblocked, and they run on `reports/map_region`** — the "wait for the final post-mitigation map" condition is met by the promoted baseline. ⚠ There is no post-mitigation map: A1 was demoted to a sensitivity arm 2026-08-25 (DECISIONS 2026-08-25k), so the product ships with the source-frame artifact unmitigated and documented |
| **Map extension — `reports/map_extended`** (no separate PLAN file; DECISIONS 2026-08-28b/c/e/f, SHERLOCK_RUN §C5) | Grow the abundance map west and south of circum-Chryse, on the one global R01 lattice, as a **growable** product separate from the frozen 26-tile `map_region`/`map_a1` | **ACTIVE.** ✅ **Round 1 SHIPPED 2026-08-28: 35 tiles, lon[−24,−4] lat[20,48]** — 27 rendered (job 41110268, 0.68–1.10 h/tile on pinned 2080 Ti) + 8 adopted from `map_region`; verified ALL CLEAN (105 rasters sha256); mosaics 10,370×7,407 with the footprint closing exactly (`76,280,030 = 35×1479² − 280,405`); external `.ovr`/`.aux.xml` for viewing (stretch 0→0.0446). **The adopted↔new join is measurably seamless** (Δ/sd −0.017, smaller than every within-arm control), so the head pin held. ⏳ **Round 2 PLANNED, not submitted: lon[−56,−4] lat[16,48]** = 104 tiles, 35 done, **69 to render** — ~50 GPU-h, all 69 verified published, **128.4 GB** of CTX zips. ⚠ Unlike round 1 this moves *toward* the training data: **14 of the 39 cohort images** fall in the new western block, against 2 in the whole of round 1 |
| [PLAN_MapValidation.md](PLAN_MapValidation.md) | **Validating the abundance map against independent data** — five experiments, five notebooks: geologic unit (Tanaka SIM3292), crater degradation (Robbins 2012), CTX source illumination, thermal (Fergason TI / TES TI / IRTM rock abundance / THEMIS night IR), and Rodriguez-2016 deposits | **ACTIVE — planned 2026-08-28, nothing built yet** (DECISIONS 2026-08-28g). Eleven design decisions ruled by Brian. Reads **one new growable product `reports/map_union`** (54 tiles = `map_region` 26 + `map_extended` 35 − **8 overlapping**, verified byte-identical, so dedup needs no merge policy) so that growing the map edits no analysis. ⚠⚠ **The striping artifact is NOT controlled for — caveat in text only** (Brian: not confident in how it has been done so far); quantifying/correcting it is a **separate investigation**, with notebook 32 as its entry point, so every contrast in 30/31/33 is an **upper bound** on the geologic signal. Three targets throughout: `abundance`, `prob_raw`, rich-fraction at `prob>=0.5`; native 160 m cells but **significance never from pixel count**. Verified while planning: SIM3292 reads via `pyogrio` (⚠ no `fiona`; ⚠ **Robinson** CRS; ⚠ invalid geoms) = 67 polygons / 16 units in the union; **Robbins is already on disk** (⚠ **latin-1**, not UTF-8) with `DEGRADATION_STATE` at **90% coverage for D≥3 km**; **Liu 2024 Zenodo is Restricted (403)** so Robbins is the catalog; the SeamMap already carries the illumination angles and streams over `/vsizip/vsicurl/` (**no 1.8 GB download**); and **all four thermal products verified reachable** — the **Fergason quantitative TI shares the CTX clon_0 sphere**, finally unblocking PLAN_RegionalMap **leg 2**. ⚠ `validation_rasters.region_bounds_lonlat` is hardcoded to the 26-tile block and must become union-derived |

## ✅ CLOSED / SHIPPED plans (chronological program arc)

| Plan | Phase | Status / outcome |
|---|---|---|
| [PLAN_FBuild.md](PLAN_FBuild.md) | The 907-frame regional F build (per-frame ISIS → H1 centering → H4 overlap leveling → composite + 6 gates) | **CLOSED — HARD ABORT 2026-07-30 (Brian)**. Ran end to end on 906/907 frames; §0.1 fallback invoked, **A1/mosaic map remains the deliverable**. F reduces within-tile striping (sd/mean 0.827→0.536; windowed partition η² 0.121→0.087) but **degrades between-place level coherence**: vs HiRISE labels, sd(log₁₀ pred/label) = mosaic **0.170** vs resid 0.371 / pfree 0.532, ratio spread 5.1× vs 32.5× / **189.6×**, and leveling makes it worse in every variant (1.13×/1.62×/1.26× vs unleveled). Absolute η²≤0.05 met by nothing incl. the mosaic. Root cause = the **2026-07-23 audit's predicted within-frame ramp**, materialised: per-frame DC operators cannot touch a within-frame gradient; the per-step term is patchy (b per lon tercile +0.203/−0.003/+0.433) and overlap data cannot separate a regional gradient from within-frame structure (r=0.998). Retained: `src/leveling.py` saturation-immune metric + lean guards + verdict-margin fix (general); diagnosis + gate tables in DECISIONS 2026-07-30/2026-07-30b. |
| [PLAN_StripingArtifact.md](PLAN_StripingArtifact.md) | The regional-map rectangular-block artifact: cause + mitigation | **CLOSED 2026-08-25 — cause SOLVED, NO mitigation survives.** ⚠⚠ **A1 was DEMOTED from shipped mitigation to sensitivity arm on 2026-08-25 (Brian; DECISIONS 2026-08-25k), reversing the 2026-07-30 ruling.** The baseline `reports/map_region` is THE product; the artifact ships **unmitigated** as a documented caveat (baseline window-median η² 0.1444 at ratio 1.599 over its own rotation null). Cause SOLVED 2026-06-18d (CTX source-frame radiometry × the embedder's fixed /255). **Why A1 was demoted:** it wins only on raw η² and the null-relative version of that same metric says the win is not a de-striping — on the pilot crop raw −44.2%, excess −42.0%, but **ratio +9.3%**; its Tier-1 ECE is **0.0523, FAILING the 0.05 gate the baseline passes at 0.0204** (force-banked); it is no better thermally (better on 10/26 tiles); and it **manufactures** frame-shaped blocks on 9/26 tiles, predictably from its own per-frame gain (Spearman(gain, mean Δ) = +0.490, p 1.4e-4 — notebook 29 §2d). This also discharges review finding **R06** ("'A1 is the shipped mitigation' is not backed by any artifact") — the artifact now exists and does not back it. A gain-capped A1 is untested and logged as **v3** material. Full ledger + the earlier re-derivation: ⚠ **η² and ΔAUC RE-DERIVED at the 2026-08-25 rebuild.** Raw η² and skill both improved: like-for-like on the E8_N44 pilot crop 0.2327 → 0.1298 = −44% (banked −28%), regional window median 0.1444 → 0.1145 = −21%, skill cost −0.0024 (banked −0.024), THEMIS-ρ cost none (per-tile median ρ 0.0653 → 0.0654). **⚠ But η² RELATIVE to its own rotation null did not improve: median ratio 1.599 → 1.639, A1 better on only 106/234 windows and 9 of 26 tiles get WORSE on raw η² (worst E-12_N32 0.207 → 0.371).** A1 renormalises per frame and narrows the BULK of the field (`prob_raw` IQR ratio 0.85, sd ratio 1.03 -- NOT a uniform compression: the tails widen slightly), so it lowers the geological floor along with the artifact — the raw reduction is real but is NOT evidence the artifact shrank relative to geology. This is the quantitative statement of "partial". The banked 0.196→0.141 / −0.024 pair is superseded and NOT comparable — different lattice, different A1 definition, different prevalence, and A1 had never been rendered as a map (R06).** Historical figures below are pre-rebuild: **F input-mapping leg closed** (5 mappings; log-minnaert = skill-gate PASS +0.0067 but η² 0.179, blocks visible — notebooks 26–28, DECISIONS 2026-07-05b/c). **Review 2026-07-05d amended the verdict**: post-minnaert overlaps agree to 4% (not 10%) — the embedder (5–20× amplifier, no cross-frame loss term) + one anomalous frame are the real floor → opened the Brian-approved **invariance & leveling docket H1–H6** (PLAN "PHASE 2" section: H1 log-median centering ~1 h → H2 nuisance-subspace removal → H3 consistency-regularized head → H4 overlap-constrained leveling of per-frame predictions (F-mode; ≠ the ruled-out D); decision rule η² ≲ 0.05 at skill ≥ −0.02 reopens the 907-frame build). **H1 DONE 2026-07-07 — both gates PASS** (per-frame log-median centering: η² median 0.179→**0.081** / partition 0.277→0.128, beats A1 0.141; skill Δ −0.0139; prediction overlap 0.073 < input 0.102 → embedder amplification killed). **H2 DONE 2026-07-09 — FAIL** (embedding nuisance-subspace removal: even k=64, removing 94% of between-frame embedding-diff variance, leaves partition η² 0.131 ≈ H1's 0.128 and *worsens* overlap; skill collapses with k; refuted — the artifact is not a fixed low-rank linear subspace). **H3 DONE 2026-07-09b — FAIL** (consistency-regularized head: the penalty DOES reduce η² monotonically — λ=100 partition η² 0.035 crosses the 0.05 bar, amplification killed — but only by collapsing the head's global dynamic range, so skill degrades on the same axis; no Pareto point clears both gates, knee λ≈3 leaves η² unchanged). H2+H3 ⇒ per-frame prediction-block variance is not separable from geology by any data-driven invariance instrument. H1 remains the baseline (η² 0.081). **H4 PILOT DONE 2026-07-09b — PASS mechanically** (overlap-constrained *post-hoc* per-frame additive prediction leveling: full offsets take partition η² 0.128→**0.0505** and HALVE the held-out leave-one-edge-out disagreement 0.074→**0.035**, with skill preserved by construction — the first PHASE-2 lever to reach the bar without collapsing skill; H1+H4 is the first stack to hit the reopening bar). **⚠ trend-guard caveat**: 58% of offset variance is a smooth plane the 7-frame pilot can't cleanly split from a real regional gradient (residual-only leaves η² 0.0595) → resolves only on the dense build (Brian ruled 2026-07-09b: trust full offsets, defer the split to the build). **H4 leg-B skill instrument DONE — PASS** (`f_h4_legb.py`, notebook 28 §9): applying the offsets costs Δ pooled PR-AUC −0.0104 (within −0.02 gate), stays +0.019 above the mosaic, Δ per-image AUC exactly 0 — skill preserved on real LOIO preds (⚠ graph fragmented at leg-B scale, 21 components ⇒ mostly within-obs). **Next = build-prep part B (H1 centering statistic) + ESP_053989 recheck + THEMIS-ρ leg on the leveled pilot map, then the reopening call** — checklist = [PLAN_FBuild.md](PLAN_FBuild.md) §0  **PHASE 2 CONCLUDED 2026-07-30: A1 stood as the mitigation** — ⚠ **and was DEMOTED 2026-08-25, see the head of this row.** H1+H4 cleared the pilot bar but the 907-frame F build was HARD-ABORTED (Brian) — it reduces within-tile striping yet degrades between-place level coherence vs HiRISE labels (see [PLAN_FBuild.md](PLAN_FBuild.md), DECISIONS 2026-07-30b). No further striping lever is open; the residual artifact ships as a documented caveat. |
| [PLAN_H4_Leveling.md](PLAN_H4_Leveling.md) | The H4 row of PLAN_StripingArtifact PHASE 2 expanded into a self-contained plan: solver, pre-declared non-circular measurement design (leave-one-edge-out edge-CV; pooled skill metrics since per-image AUC is provably blind to leveling), pilot recipe, build-prep verify items | **CLOSED 2026-07-30 — pilot + leg-B PASSED, but the 907-frame build they justified was HARD-ABORTED (see PLAN_FBuild); H4 is shelved, not shipped**. `lv.solve_offsets*` stays in the codebase unused; §5 sketch promoted to PLAN_FBuild 2026-07-13 |
| (CLAUDE.md §3–4) | Stages 0–3 — manifest, reproject detections, CTX retrieval, co-registration | shipped (commits `0770e2a`/`8e8645d`/`ed9003e`) |
| (CLAUDE.md §4) | Stage 4 — label gen on nested ×2 grid | shipped (`896cdef`; DECISIONS 2026-05-23) |
| [PLAN_Stage4b.md](PLAN_Stage4b.md) | Stage 4b — per-tile CTX texture features | shipped (`014f645`; 9 feature families, 60 cols) |
| [PLAN_Stage5.md](PLAN_Stage5.md) | Stage 5 — leave-image-out splits + packaging | shipped (`aa6cd74`; `loio_9fold` + `loio_3fold_balanced`) |
| [PLAN_Stage5b.md](PLAN_Stage5b.md) | Stage 5b — binary rich/poor reframing | shipped (folded into the modeling track) |
| [PLAN_Stage5c.md](PLAN_Stage5c.md) | Stage 5c — within-image k-fold CV | shipped 2026-05-27 (signal-floor branch confirmed) |
| [PLAN_modeling.md](PLAN_modeling.md) | Week 3 modeling — GBM baseline + two-stage | shipped v1; results [docs/modeling_results.md](docs/modeling_results.md) §1–8 |
| [PLAN_NewDetections.md](PLAN_NewDetections.md) | vClaire v2 — denser 40-img detection set | shipped → `dataset_v2/` (38 images); the dataset all later work uses |
| [PLAN_ModelImprovement.md](PLAN_ModelImprovement.md) | Model-improvement Phases A/B/C | shipped & **historical** (compression diagnosis, hurdle variants) |
| [PROMOTION_QUEUE.md](PROMOTION_QUEUE.md) Part B | Stage 6 — feature augmentation / per-image standardization | done (6a dev-PASS deferred; 6b strict-FAIL but mechanism validated; 6c soft-PASS) |
| [PLAN_Compositional.md](PLAN_Compositional.md) | Stage 7 — compositional study (HiRISE 3 bands) | Stage 7.0/7a/7c/7d + Tier 1/2 done; **PARKED** (compositional taken off the docket at the 2026-06-10 usability pivot); [docs/compositional.md](docs/compositional.md) |
| [PLAN_CNN.md](PLAN_CNN.md) | W2 — CNN on context patches | **CLOSED 2026-06-12 → superseded by PLAN_FM** (the Fang-ViT probe beat the CNN) |
| [PLAN_FM.md](PLAN_FM.md) | Post-foundation-model program | recipe **FROZEN 2026-06-12** (`fw_emb_mlp_ens3_gem96_S32_fa_gt_1e-2`) + productized → `DeployableHead`; closed |
| [PLAN_ModelUsability.md](PLAN_ModelUsability.md) | Make the model usable (Tier-1 binary map → calibrated abundance) | the 2026-06-10 pivot umbrella; **delivered via PLAN_Calibration + PLAN_RegionalMap** |
| [PLAN_Calibration.md](PLAN_Calibration.md) | De-compress / calibrate the abundance outputs | Stage 0 + Stage 1 **SHIPPED** (`CalibrationLayer`, a09f06b); Stage 2 **CLOSED** (retraining ceiling = 5 m/px CTX floor) |

## 📋 Meta / reference docs (not plans)

- [CLAUDE.md](CLAUDE.md) — the build spec (authoritative for scope).
- [DECISIONS.md](DECISIONS.md) — the running log; authoritative for dates/commits/numbers.
- [README.md](README.md) — setup + how to run each stage + sweeps.
- [PROMOTION_QUEUE.md](PROMOTION_QUEUE.md) — forward-looking docket of dev-validated changes awaiting full-v2 confirmation.
- [SHERLOCK_RUN.md](SHERLOCK_RUN.md) — operational runbook for the Sherlock GPU regional runs (active reference).
- [HANDOFF_NEXT_SESSION.md](HANDOFF_NEXT_SESSION.md) — ⚠️ stale (last 2026-06-15); the live session state lives in the memory `project_state_*` notes, not here.

## Conventions

- **Each plan is self-contained**: a future session can pick up a phase from its `PLAN_*.md` + the
  relevant DECISIONS entries without the others.
- **When a plan goes stale**, update it in the same change that diverges (a dated note at the top of
  the affected section); don't let it silently drift. Mark superseded plans with a top banner pointing
  to the successor (see PLAN_CNN.md, PLAN_StripingArtifact.md §1–6 for the pattern).
- **Open questions** in a plan's §10/end are the things execution should NOT pre-decide — surface them
  via `AskUserQuestion` (collaboration rule #1).
- This file is the index; keep it current when a plan opens, closes, or is superseded.
